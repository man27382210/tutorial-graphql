import { createStyles, withStyles } from '@material-ui/core/styles'

export const searchStyle = withStyles(() => createStyles({
  pre: {
    margin: 0,
  },
  infoPaper: {
    padding: 20,
    minHeight: 100,
  }
}))
